# dependencies
node_modules

# build output
dist
dist-ssr
*.local
*.tsbuildinfo

# vercel
.vercel

# env
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editors / os
.DS_Store
Thumbs.db
.idea
.vscode/*
!.vscode/extensions.json

# Raw frame source material (~403 MB). Nothing in src/ imports it — the hero
# canvas only reads src/assets/hero-frames. Keep it out of git.
src/backgrounds/
